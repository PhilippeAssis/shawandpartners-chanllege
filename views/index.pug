doctype html
html(lang="en")
  head
    meta(name="viewport", content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no")
    title Shawand Partners - The full-stack challenge
    link(rel='stylesheet', href="css/styles.css")

  body
    .wrap#app
      h1 Your word is a palindrome?
      .form-group(v-bind:class="{ success }")
        input.form-control(v-model="palindrome", placeholder="Type a word")
      .result {{ result }}

      script(src="js/scripts.js")
